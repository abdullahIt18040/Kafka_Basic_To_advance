
## number of consumers in a group should be  equal noumber of  pationtion
## Consumer Offset কী?

## Consumer Group কী?
Kafka-তে Consumer Group হলো এমন একটি ব্যবস্থা যেখানে একাধিক consumer মিলেই একটি group হিসেবে কাজ করে।
একটি topic-এর মেসেজগুলো Kafka সেই গ্রুপের consumer গুলোর মধ্যে ভাগ করে দেয়।

📍এর মানে:

একটা consumer group → বহু consumer কাজ ভাগাভাগি করে নেবে

মেসেজ ডুপ্লিকেট হবে না (এক consumer একবার মেসেজ নেবে)

✅ ধাপ ১: কিছু মেসেজ প্রডিউস করুন (আগে না করলে)

টার্মিনালে লিখুন:

C:\kafka> .\bin\windows\kafka-console-producer.bat --topic my-first-topicone --bootstrap-server localhost:9092


এখন কিছু মেসেজ লিখে Enter চাপুন।

✅ ধাপ ২: একটি consumer group দিয়ে consumer চালান

ধরুন আপনার গ্রুপের নাম my-group, তখন লিখুন:

C:\kafka> .\bin\windows\kafka-console-consumer.bat --topic my-first-topicone --group my-group --bootstrap-server localhost:9092


👉 এটা my-group নামে একটি নতুন group তৈরি করবে (যদি আগে না থাকে)।

✅ ধাপ ৩: দ্বিতীয় consumer চালান একই গ্রুপে

আরেকটি নতুন টার্মিনাল খুলে আবার লিখুন:

C:\kafka> .\bin\windows\kafka-console-consumer.bat --topic my-first-topicone --group my-group --bootstrap-server localhost:9092


📍এখন আপনার দুইটি consumer একই গ্রুপে আছে।
Kafka প্রতিটি মেসেজ একটি consumer-এ পাঠাবে। (লোড ব্যালেন্সিং হবে)

ধাপ ৪: গ্রুপের অবস্থা দেখতে চাইলে
C:\kafka> .\bin\windows\kafka-consumer-groups.bat --describe --group my-group --bootstrap-server localhost:9092


এটি দেখাবে:

তথ্য	অর্থ
Topic	কোন topic থেকে মেসেজ যাচ্ছে
Partition	কোন পার্টিশন
Current Offset	কত পর্যন্ত পড়া হয়েছে
Lag	এখনও কত মেসেজ বাকি
📌 কেন Consumer Group ব্যবহার করা হয়?
উদ্দেশ্য	ব্যাখ্যা
Load balancing	একাধিক consumer একসাথে কাজ ভাগ করে নেয়
Scalability	গ্রুপে আরও consumer যোগ করলে পারফরম্যান্স বাড়ে
No duplicate messages	একটি মেসেজ কেবল একটি consumer-এ যায় (group-এর মধ্যে)


Kafka-তে যখন কোনো consumer (গ্রাহক) কোনো topic থেকে data পড়ে, তখন Kafka সেই consumer কোথায় পর্যন্ত পড়েছে তা offset দিয়ে ট্র্যাক করে রাখে।

 Offset হলো প্রতিটি message-এর জন্য একটি unique number (0, 1, 2, 3, ... ইত্যাদি)।
এটা message-এর position বা index এর মতো কাজ করে।
```
 উদাহরণ:

ধরুন আপনার একটা topic আছে — orders
এতে ৫টি message আছে:

Offset	Message
0	Order #1
1	Order #2
2	Order #3
3	Order #4
4	Order #5

এখন আপনার consumer যদি প্রথম ৩টা message পড়ে,
তাহলে Kafka মনে রাখবে যে consumer-এর offset এখন 2 পর্যন্ত পৌঁছেছে।

 মানে consumer 0, 1, 2 offset পর্যন্ত message consume করেছে।

 Consumer Offset কেন দরকার:
কারণ	ব্যাখ্যা
🔁 Restart এর পর থেকে আবার শুরু করার জন্য	যদি consumer বন্ধ হয়ে যায় বা restart হয়, Kafka জানে শেষ কোন offset পর্যন্ত message পড়া হয়েছিল। ফলে consumer সেখান থেকেই আবার শুরু করতে পারে।
✅ Duplicate এড়ানোর জন্য	Offset ট্র্যাক করে রাখলে একই message বারবার পড়া হয় না।
📊 Monitoring ও Lag Check করার জন্য	Offset থেকে বোঝা যায় consumer কত পিছিয়ে আছে (lag) বা কত message এখনো consume হয়নি।
🧩 Offset কোথায় সংরক্ষণ হয়?

Kafka offset সাধারণত __consumer_offsets নামের একটি internal topic-এ সংরক্ষণ করে।
এই টপিক Kafka নিজেই manage করে।

উদাহরণ:

ধরুন consumer group-এর নাম group1
topic orders
partition 0 এর সর্বশেষ offset হলো 100

consumer যদি offset 95 পর্যন্ত পড়ে থাকে,
তাহলে consumer lag = 100 - 95 = 5 message পিছিয়ে আছে।
```
 সংক্ষেপে বাংলা সংজ্ঞা:

Consumer Offset হলো Kafka-র একটি সংখ্যা, যা বলে দেয় কোনো consumer কোন message পর্যন্ত পড়েছে।
এর মাধ্যমে Kafka consumer-এর অবস্থান ট্র্যাক করে, যাতে পুনরায় শুরু হলে বা ত্রুটি ঘটলে ডেটা হারায় না এবং ডুপ্লিকেট না হয়।


```
ধাপ ১: ফাইল আপলোড

ইউজার .xlsx ফাইল আপলোড করে।
আপনার API / Frontend ফাইলটা নিয়ে Kafka-এ পাঠায়।

textFrontend → [POST /upload] → Kafka Topic: xlsx.incoming

Kafka-এ ফাইলটা বাইট (byte[]) হিসেবে যায়।


ধাপ ২: File-Converter সার্ভিস

১২টা থ্রেড চলছে।
প্রতিটি থ্রেড Kafka থেকে একটা ফাইল নেয়।
Apache POI দিয়ে .xlsx → .csv করে।
কনভার্ট হওয়া .csv → Kafka-এ পাঠায়।

textKafka (xlsx.incoming) → [Converter 1] → Kafka (csv.validated)
Kafka (xlsx.incoming) → [Converter 2] → Kafka (csv.validated)
...
Kafka (xlsx.incoming) → [Converter 12] → Kafka (csv.validated)

১২টা ফাইল একসাথে কনভার্ট হচ্ছে!


ধাপ ৩: Validator সার্ভিস

২৪টা থ্রেড চলছে (কারণ ভ্যালিডেশন বেশি CPU লাগে)।
Kafka থেকে .csv নেয়।
প্রতি রো চেক করে:

নাম আছে?
ইমেইল @ আছে?
তারিখ DD-MM-YYYY?



javaif (email.contains("@") && date.matches("\\d{2}-\\d{2}-\\d{4}")) {
    → সঠিক → Kafka: csv.load-ready
} else {
    → ভুল → Kafka: csv.rejected
}

২৪টা CSV ফাইল একসাথে চেক হচ্ছে!


ধাপ ৪: Loader সার্ভিস

১২টা থ্রেড।
Kafka থেকে csv.load-ready নেয়।
টেম্প ফোল্ডারে .csv সেভ করে।
.ctl ফাইল তৈরি করে।
sqlldr চালায়:

bashsqlldr user/pwd@db control=load.ctl direct=true parallel=true

১২টা ফাইল একসাথে Oracle-এ লোড হচ্ছে!


ধাপ ৫: ভুল ডেটা কী হবে?

csv.rejected টপিকে যায়।
আপনি পরে দেখতে পারেন।
অথবা Alert পাঠাতে পারেন।
পরে Reprocess করতে পারেন।


Kafka কীভাবে পারফরম্যান্স বাড়ায়? (বিস্তারিত)



































ফিচারকী করেপারফরম্যান্স গেইনপার্টিশন১২টা → ১২টা থ্রেড একসাথে১২× দ্রুতঅ্যাসিঙ্ককেউ অপেক্ষা করে নাকোনো ব্লক নেইব্যাচিং৫০০ রেকর্ড একসাথে পাঠায়নেটওয়ার্ক কম লাগেকম্প্রেশনsnappy → ১০০MB → ২০MB৫× কম ডেটাKRaftZooKeeper নেই → দ্রুত৩০% কম লেটেন্সি

উদাহরণ: ১০০ ফাইল (প্রতিটি ১০MB)






























ধাপKafka ছাড়াKafka দিয়েকনভার্ট১০০ × ৪৫ সেকেন্ড = ৭৫ মিনিট১০০ / ১২ = ৯ ব্যাচ × ৮ সেকেন্ড = ৭২ সেকেন্ডভ্যালিডেট১০০ × ২ মিনিট = ২০০ মিনিট১০০ / ২৪ = ৫ ব্যাচ × ২২ সেকেন্ড = ১.৮ মিনিটলোড১০০ × ৯০ সেকেন্ড = ১৫০ মিনিট১০০ / ১২ = ৯ ব্যাচ × ১৫ সেকেন্ড = ২.২ মিনিটমোট৭+ ঘণ্টা~৬ মিনিট

৭০× দ্রুত!
```
